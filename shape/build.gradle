plugins {
    id 'com.android.library'
}

android {
    compileSdkVersion cfgs.compileSdk
}

dependencies {
    implementation "androidx.appcompat:appcompat:$cfgs.appcompatVersion"
    implementation "androidx.constraintlayout:constraintlayout:$cfgs.constraintlayout"
}

